# AllVie

AllVie is an Android file reader scaffold built from the supplied PRD. The current implementation sets up:

- Jetpack Compose + Material 3 app structure
- MVVM + Hilt dependency injection
- Room persistence for bookmarks and recents
- DataStore preferences for theme, layout mode, and storage root
- In-app PDF, image, and text readers
- External app handoff for Office formats

The project is ready to open in Android Studio. This environment does not have Gradle installed, so the wrapper and build artifacts were not generated here.
